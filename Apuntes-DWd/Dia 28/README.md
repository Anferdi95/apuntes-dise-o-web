```js

const hobbies = ["futbol", "Programar en JS", "Leer" ];

for (i=0; i< hobbies.length ; i++;){
//recorrer una lista
    const hobbie = hobbies[i];
    console.log(hobbies); //"futbol", "progrmar en JS", "Leer"

    console.log(hobbie.toUpperCase()); //poner en MAYUSCULAS
    console.log(hobbie.TolowerCase()); //poner en Mminusculas

    divContainer.innerHTML +="<span>"+ hobbies "</span>";           
    divContainer.innerHTML +="<span>"+${hobbie.toUpperCase()} "</span>";
}

console.logo(hobies[0]); // leer el elemento de la lista
console.log(cant);  //leer la cantidad de elementos

for(let i=0  i<10; i++){
    console.log("hola")  //imprime la cantidad de veces el elemento  en este caso 10 veces el hola
}

for(let i=0  i<5; i++){
    console.log("i vale" + i)  //imprime la cantidad de veces el elemento  en este caso 10 veces el y vale pero se le suma 1 (i vale 0,  i vale 1, etc...



}